# 4K 16” laptop baseline tuned for denser daily use.
# 1.75 is deliberate here: more space than 2x, while still staying readable on a 16” panel.
monitor = , preferred, auto, 1.75

xwayland {
    force_zero_scaling = true
}

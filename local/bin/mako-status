#!/usr/bin/env bash
set -euo pipefail

if ! command -v makoctl >/dev/null 2>&1; then
  printf '{"text":"","class":"enabled"}\n'
  exit 0
fi

if makoctl mode | grep -qx 'do-not-disturb'; then
  printf '{"text":"󰂛 dnd","class":"enabled"}\n'
else
  printf '{"text":"","class":"disabled"}\n'
fi

#!/usr/bin/env bash
set -euo pipefail

if command -v pulsemixer >/dev/null 2>&1; then
  exec kitty --class hyprglass-float --title "hyprglass :: audio"     sh -lc 'pulsemixer'
fi

exec kitty --class hyprglass-float --title "hyprglass :: audio"   sh -lc 'alsamixer'
